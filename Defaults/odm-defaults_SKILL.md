
## Session note 2026-07-15
Main `_Defaults.csv` is DefaultValue-only; relevancy/stage rules split to `_Relevancy_Stage_Rules.csv`;
dead/ignored rules archived per-UUID with reason in `_Dead_Ignored_Rules.csv`. `Disabled` attribute now
extracted. Always-ON flag false-branches are archived as dead (live branch kept with condition intact).
